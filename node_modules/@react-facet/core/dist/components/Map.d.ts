import { ReactElement } from 'react';
import { EqualityCheck, Facet } from '../types';
export declare type MapProps<T> = {
    array: Facet<T[]>;
    children: (item: Facet<T>, index: number, length: number) => ReactElement | null;
    equalityCheck?: EqualityCheck<T>;
};
export declare const Map: <T>({ array, children, equalityCheck }: MapProps<T>) => JSX.Element;
