import { FacetProp, Value, NoValue, EqualityCheck } from '../types';
/**
 * Hook that allows consuming values from a Facet
 * It acts as a regular react state, triggering a re-render of the component
 *
 * @param facet
 */
export declare function useFacetUnwrap<T extends Value>(prop: FacetProp<T>, equalityCheck?: EqualityCheck<T>): T | NoValue;
