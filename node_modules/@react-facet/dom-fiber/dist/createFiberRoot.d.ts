import { FacetFiberRoot, ReactFacetReconciler } from './types';
/**
 * @private consider using render instead
 */
export declare const createFiberRoot: (reconciler: ReactFacetReconciler) => (container: HTMLElement) => FacetFiberRoot;
